Create implementation-plan-sixteen.md in the prompts directory.

Create an implementation plan to handle the following feature creation or adjustment.

Output the plan to the implementation plan file.

### Overview

Let's expand the functionality of tryout manager.

### Tasks

- When clicking the arrow to send a player to a team, the popup can sometimes fall off the screen. Adjust so that if the overlay would find itself being cut off by the screen that it instead anchors the bottom of the overlay to the arrow, allowing me to select any team in the overlay.
